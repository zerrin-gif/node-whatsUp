extends layout

block layout-content
  div.View.UserView
    h1.Banner Howdy, #{userProfile}!
    div.Message
      div.Title
        h3 Making Us the Best
        h1 Teammate Profile
      h2
        pre.Details=JSON.stringify(userProfile, null, 2)
    div.NavButtons
      a(href="/logout")
        div.NavButton Log out
